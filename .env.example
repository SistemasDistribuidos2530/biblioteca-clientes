# Configuración de ejemplo para el Proceso Solicitante (PS)
# Copiar este archivo a .env y ajustar valores según el entorno

# Dirección del Gestor de Carga (GC)
# Formato: tcp://IP:PUERTO
# Para ambiente local: tcp://localhost:5555
# Para ambiente distribuido (M3 -> M1): tcp://10.43.101.220:5555
GC_ADDR=tcp://10.43.101.220:5555

# Timeout de respuesta del GC en segundos
# Valor recomendado: 2.0 - 5.0
PS_TIMEOUT=2.0

# Secuencia de backoff exponencial (separada por comas)
# Tiempos de espera entre reintentos en segundos
# Ejemplo: 0.5,1,2,4 significa esperar 0.5s, luego 1s, luego 2s, luego 4s
PS_BACKOFF=0.5,1,2,4

# Clave secreta compartida para firmas HMAC
# IMPORTANTE: NO subir la clave real al repositorio
# En producción usar un secreto fuerte generado con: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=clave-compartida-secreta

